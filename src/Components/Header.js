import logo from '../assets/logo.png'

function Header (){
    return (
    <header>
        <img src={logo} alt='kasa'/>
        <p>Accueil</p>
        <p>A Propos</p>
    </header>
)}

export default Header